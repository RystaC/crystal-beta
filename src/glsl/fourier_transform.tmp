#version 450

layout(std430, set = 0, binding = 0) buffer readonly Input {
    float input[];
};

layout(std430, set = 0, binding = 1) buffer writeonly Output {
    float output[];
};

const float PI = 3.141592653589793;

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;
void main() {
    uint tid = gl_GlobalInvocationID.x;
    if(index >= output.length()) return;

    for(int = 0; i < input.length(); ++i) {

    }
}